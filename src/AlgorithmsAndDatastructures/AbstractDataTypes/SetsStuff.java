package DataStructures.AbstractDataTypes;

/**
 * These are basically unordered Arrays without duplicates
 * Sets (German: Mengen) in mathematical sense
 * Operations: add, remove, contain (searches for stuff), union (merges two sets)
 *
 * => use-case: no duplicate wanted, filtering for values (without duplicates)
 */
public class SetsStuff {
}
